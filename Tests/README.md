You must have PHPUnit installed to run these test cases. Instructions for
installing PHPUnit are at http://www.phpunit.de/.

To run a test use the `phpunit` command in the `Test` directory:

```bash
phpunit PublicationTest
```
